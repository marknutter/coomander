# AGENTS.md — Coomander

All project and feature documentation now lives in **[README.md](./README.md)**.

Read it first — it's the single source of truth and covers:

- **What's already built** (don't reimplement it): auth, database, email, payments, UI component library, blog/MDX, the three-tier docs system
- Tech stack, env vars, file structure, and the standard API route pattern
- **Critical rules** and **common mistakes** to avoid
- Local dev (Docker + Tailscale), Cloudflare Workers deploy, and how to create/sync Coomander projects

Consolidated into the README so every tool and harness reads from one place.
